<%= form_for(home_test) do |f| %>
  <% if home_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(home_test.errors.count, "error") %> prohibited this home_test from being saved:</h2>

      <ul>
      <% home_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ServiceName %>
    <%= f.text_field :ServiceName %>
  </div>

  <div class="field">
    <%= f.label :Amount %>
    <%= f.text_field :Amount %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>

  <div class="field">
    <%= f.label :Time %>
    <%= f.date_select :Time %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
